from functionsWithReturn import *
# we can import functions from other files by using the sentence above. also multiple functions can be imported
# with a comma like above "multi, sub". we can also use "*" instead of multi, sub etc. which will import all data
# from the file named.
y = int(input("enter the first value:"))
z = int(input("enter the second value:"))

print(multi(9,0))
print(sub(9,8))
print(x)
print(multi(y,z))
